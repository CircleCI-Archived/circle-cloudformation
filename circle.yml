machine:
  python:
    version: 2.7.9
dependencies:
  post:
    - pip install awscli
test:
  override:
    - jq '.' template.json
deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
